---
- hosts: localhost
  gather_facts: no
  tasks:
    - name: Create security group sg-postgres
      ec2_group:
        name: m5-sg-postgres
        vpc_id: "{{ vpc.vpc.id }}"
        region: us-west-1
        rules:
          - proto: tcp
            ports:
            - 80
            - 443
            - 1024-65535
            cidr_ip: 0.0.0.0/0
            group_name: m5-sg-postgres
